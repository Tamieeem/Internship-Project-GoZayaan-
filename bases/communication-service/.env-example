# Database Configuration
DB_NAME =
DB_USER =
DB_PASSWORD =
DB_HOST =
DB_PORT =

# Environment Configuration
DEBUG = True
SECRET_KEY =

# JWT Configuration
JWT_SECRET_KEY =
JWT_ALGORITHM =
JWT_ISSUER =
JWT_ACCEPTED_AUDIENCES =
JWT_ACCESS_TOKEN_LIFETIME_MINUTES =

# RabbitMQ Configuration(change user & password)
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Celery Configuration
CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672// #in local development, use localhost instead of rabbitmq, and docker run use rabbitmq
